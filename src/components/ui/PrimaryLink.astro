---
interface Props {
  href: string;
  text: string;
}

const { href, text } = Astro.props;
---

<a
  href={href}
  class="p-4 inline-flex bg-primary items-center text-dark-blue font-medium hover:cursor-pointer hover:bg-primary/80"
>
  <slot />
  <span class="ml-3">{text}</span>
</a>
